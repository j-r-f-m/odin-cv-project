import React, { Component } from "react";

class Header extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
      <div id="header">
        <h2 id="header--title">CSV Creator</h2>
      </div>
    );
  }
}

export default Header;
